// Plugin installation: GitHub fetch, archive extraction, manifest validation,
// consent preview, atomic install. Real implementations in Phase 3 (US1).

pub mod github;
pub mod archive;
pub mod manifest;
