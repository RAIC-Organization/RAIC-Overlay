# ============================================================================
# Window Detection Settings (all optional - defaults to Star Citizen)
# ============================================================================

# Optional: Target window name for overlay attachment (default: "Star Citizen")
# The overlay will attach to windows containing this string in their title
# Example values:
#   - "Star Citizen" for Star Citizen game
#   - "Notepad" for testing with Notepad
# TARGET_WINDOW_NAME=Star Citizen

# Optional: Target process name for window detection (default: "StarCitizen.exe")
# Used for three-point verification to ensure correct window matching
# Example values:
#   - "StarCitizen.exe" for Star Citizen
#   - "notepad.exe" for testing with Notepad
# TARGET_PROCESS_NAME=StarCitizen.exe

# Optional: Target window class for window detection (default: "CryENGINE")
# Used for three-point verification to ensure correct window matching
# Example values:
#   - "CryENGINE" for Star Citizen (CryEngine-based)
#   - "Notepad" for Windows Notepad
# TARGET_WINDOW_CLASS=CryENGINE

# ============================================================================
# Debug and Logging Settings (all optional)
# ============================================================================

# Optional: Enable debug border to visualize window boundaries
# Set to "true" to show a 10px red border around the overlay
VITE_DEBUG_BORDER=false

# Optional: Set logging level for the application
# Values: TRACE, DEBUG, INFO, WARN, ERROR (default: WARN)
# DEBUG/INFO also enable console output; WARN/ERROR only log to file
RAIC_LOG_LEVEL=DEBUG
